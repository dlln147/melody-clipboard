## Summary

<!-- What does this PR change, and why? -->

## Related issue

<!-- Closes #... , or "N/A" -->

## Checklist

- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run format:check` passes
- [ ] `npm run test` passes
- [ ] `npm run build` passes
- [ ] `cargo fmt -- --check` passes (in `src-tauri/`)
- [ ] `cargo clippy --all-targets -- -D warnings` passes (in `src-tauri/`)
- [ ] `cargo test` passes (in `src-tauri/`)
- [ ] I added/updated tests for any conversion-logic or MIDI-encoding changes
- [ ] I did not commit build output, local config, or the app version (unless this PR is specifically a release PR)

## Screenshots (if UI changes)

<!-- Before/after screenshots for any visible UI changes -->
